{
  "name": "agent-layer-playground",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "sync-agent-layer": "node scripts/sync-agent-layer.mjs"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "@agent-layer/core": "file:vendor/agent-layer-core",
    "@agent-layer/hono": "file:vendor/agent-layer-hono"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
